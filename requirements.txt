# core
pandas==2.2.3
numpy==2.1.1
plotly           # 支援 Python 3.13
scikit-learn==1.5.2
scipy==1.14.1          # 有 wheel

# models
xgboost==2.1.1
lightgbm==4.5.0

# tracking / config
mlflow>=2.19.0
pyyaml==6.0.2

# api / app
fastapi==0.115.5
uvicorn[standard]==0.32.0
streamlit==1.40.1

# nlp / rag（後面章節用到）→ 先註解或降版 faiss
# sentence-transformers==3.2.1
# faiss-cpu==1.8.0      # 改成 1.8.0（有 wheel），或暫時註解

# utilities
tqdm==4.66.5
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.24.1

# quality
black==24.10.0
ruff==0.6.9
pre-commit==4.0.1

# kaggle
kaggle==1.8.4
python-dotenv==1.0.1

# 強制只用 binary wheel（避免 source build）
--only-binary :all: